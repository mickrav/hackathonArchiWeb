<html>
	<head>
		
	</head>
	<body>
		<h1>Reservation Page </h1>
		<div class="jumbotron">
			<h1><%= title %></h1>
			<ul>
			    <% for(var i=0; i<items.length; i++) { %>
			        <li>
			            <a href='items/<%= items[i] %>'>
			                <%= items[i] %>
			            </a>
			        </li>
			    <% } %>
			</ul>
		</div>
	</body>
</html>